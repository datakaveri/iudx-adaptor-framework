apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc
  labels:
    type: minio-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "ebs-storage-class"
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      type: minio-pv

